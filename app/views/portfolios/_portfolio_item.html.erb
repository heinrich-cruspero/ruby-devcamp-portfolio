<h2><a href="<%= show_portfolio_path(portfolio_item.id)%>" ><%= portfolio_item.title %></a></h2>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.thumb_image if portfolio_item.thumb_image %>
<a href="<%= edit_portfolio_path(portfolio_item.id) %>">EDIT</a>
<%= link_to 'Destroy', portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?' } %>